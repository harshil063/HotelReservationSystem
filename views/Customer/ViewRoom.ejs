
    
<%- include('../partials/Customer/header.ejs') %>
    <div class="container">
        <!-- @*<form asp-controller="Customer" asp-action="cust_viewroom">
                <p>
                    Title: <input type="text" name="SearchString" />
                    <input type="submit" value="Filter" />
                </p>
            </form>*@ -->
        
        <div class="content-wrapper">
    
            <!-- Main content -->
            <section class="content">
                <div class="container-fluid">
                    <h3 class="text-center ">Search</h3>
                    <div class="row">
                        <div class="col-md-8 offset-md-2">
                            <form asp-controller="Customer" asp-action="cust_viewroom">
                                <div class="input-group">
                                    <input type="search" name="SearchString" class="form-control form-control-lg" placeholder="Search your room Here">
                                    <div class="input-group-append">
                                        <button type="submit" class="btn btn-lg btn-default">
                                            <i class="fa fa-search"></i>
                                        </button>
                                    </div>
                                </div>
                            </form>
                            <!-- @if (Model.Count() == 0)
                            {
                                <p class="blockquote" style="color:red; text-align:center">@ViewBag.emptyerr</p>
                            } -->
                        </div>
                    </div>
                    
                </div>
            </section>
            
        </div>
            <!-- @*@if (ViewBag.emptyerr != null)
            {
                <p class="alert alert-danger" style="width:280px;">@ViewBag.emptyerr</p>
            }*@ -->
            
                
            
            <div class="container pt-5">
                <div class="d-flex flex-column text-center mb-5">
                    <h4 class="text-secondary mb-3">Pet Blog</h4>
                    <h1 class="display-4 m-0"><span class="text-primary">Updates</span> From Blog</h1>
                </div>
                <div class="row pb-3">
                    
                    <% var i = 1 %>
                    <% data.forEach((item)=>{ %>
                    <div class="col-lg-4 mb-4">
                        <div class="card border-0 mb-2">
                            <img class="card-img-top c" src="/uploads/<%= item.room_photo %>" alt="">
                            <div class="card-body bg-light p-4">
                                <h4 class="card-title text-truncate" style="text-align: center;"><%= item.room_type %></h4>
                                <div class="d-flex mb-3">
                                    <small class="mr-2"><i class="fa fa-user text-muted"></i> Admin</small>
                                    <small class="mr-2"><i class="fa fa-folder text-muted"></i> Web Design</small>
                                    <small class="mr-2"><i class="fa fa-comments text-muted"></i> 15</small>
                                </div>
                                <p ><%= item.room_desc %>
                                <!-- <a class="font-weight-bold" href="">Read More</a> -->
                                <h3>&#8377 <%= item.room_price %> / Night</h3>
                                <!-- <p><%= item._id%></p> -->
                                <div class="product-bottom-details">
                                    <!-- <a href="BookRoom/<%= item._id%>" class="btn btn-outline-primary btn-block">Book Now <i class="fas fa-eye"></i></a> -->
                                    <a href="BookRoom/<%= item.room_type%>" class="btn btn-outline-primary btn-block">Book Now <i class="fas fa-eye"></i></a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            



        </div>
        <%- include('../partials/Customer/footer.ejs') %>